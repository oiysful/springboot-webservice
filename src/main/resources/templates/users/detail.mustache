<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/users/detail.css">
    <title>IAN Spring Boot Webservice</title>
</head>
<body>
    <canvas id="particle-canvas"></canvas>
    <main class="main-section">
        <header>
            <button id="user-detail-close-btn" onclick="history.back()"></button>
        </header>
        <article>
            <img src="{{user.picture}}" alt="user-picture" id="user-picture">
            <span id="user-name">{{user.name}}</span>
            <span id="user-email">{{user.email}}</span>
            <span id="user-role">{{role-name}}</span>
        </article>
        <footer>
            <img src="/css/img/github-mark.png" alt="#" id="github-mark">
            <a href="https://github.com/oiysful/springboot-webservice" id="repository-link">Github Repository</a>
        </footer>
    </main>

    <script src="/js/common/background.js"></script>
</body>
</html>