<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/posts/list.css">
    <title>IAN Spring Boot Webservice</title>
</head>

<body>
    <main class="main-section">
        <header>
            <img src="{{user.picture}}" alt="#" id="user-icon">
            <div id="user-info-modal" class="hidden">
                <span id="user-name">{{user.name}}</span>
                <span id="user-email">{{user.email}}</span>
                <a href="/user/{{user.id}}">마이페이지</a>
                <a href="/logout">로그아웃</a>
            </div>
        </header>
        <article>
            <div id="find-items">
                <form action="/posts/search">
                    <select id="find-post-opt" class="option-items" name="select">
                        <option value="title">제목</option>
                        <option value="author">작성자</option>
                        <option value="content">내용</option>
                    </select>
                    <input type="text" name="keyword" id="find-post-input" class="option-items"
                        placeholder="검색어를 입력하세요">
                    <input type="submit" id="find-post-btn" class="option-items find-post-btn" value="검색">
                </form>
                <a href="/posts/save" id="post-create-btn" class="option-items find-post-btn">작성</a>
            </div>
            <table id="post-table">
                <thead id="post-table-head">
                    <tr>
                        <th id="table-no">No</th>
                        <th id="table-subject">Subject</th>
                        <th id="table-author">Author</th>
                        <th id="table-mdf-at">Modified At</th>
                    </tr>
                </thead>
                <tbody>
                    {{#postsPage.list}}
                    <tr>
                        <td class="table-item-center">{{id}}</td>
                        <td class="table-item-bold"><a href="/posts/{{id}}">{{title}}</a></td>
                        <td class="table-item-center">{{author}}</td>
                        <td class="table-item-center">{{modifiedDate}}</td>
                    </tr>
                    {{/postsPage.list}}
                </tbody>
            </table>
            <ul id="posts-paging">
                <li id="posts-pre-page" class="page-arrow"
                    {{^postsPage.hasPrevPage}}style="background-image: none; width: 20px;" {{/postsPage.hasPrevPage}}>
                    {{#postsPage.hasPrevPage}}
                    <a href="?page={{postsPage.prevPage}}" role="button"></a>
                    {{/postsPage.hasPrevPage}}
                </li>
                <li id="page-num">
                    <span><b>{{postsPage.currentPage}}</b> / {{postsPage.totalPage}}</span>
                </li>
                <li id="posts-next-page" class="page-arrow"
                    {{^postsPage.hasNextPage}}style="background-image: none; width: 20px;" {{/postsPage.hasNextPage}}>
                    {{#postsPage.hasNextPage}}
                    <a href="?page={{postsPage.nextPage}}" role="button"></a>
                    {{/postsPage.hasNextPage}}
                </li>
            </ul>
        </article>
        <footer>
            <img src="/css/img/github-mark.png" alt="#" id="github-mark">
            <a href="https://github.com/oiysful/springboot-webservice" id="repository-link">Github Repository</a>
        </footer>
    </main>

    <script src="/js/common/common.js"></script>
    <script src="/js/users/header.js"></script>
</body>

</html>