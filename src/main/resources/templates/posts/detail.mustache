<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>IAN Spring Boot Webservice</title>
</head>
<body>
    <main>
        <header>
            <span id="greeting">Welecome!</span>
            <a href="/user/{{user.id}}" id="user-info">{{user.name}}</a>
            <a href="/logout" id="logout">Logout</a>
        </header>
        <article>
            <div id="post-details">
                {{post.title}}
                {{post.author}}
                {{post.content}}
            </div>
            <a href="/" role="button" class="post-details-btn">목록</a>
            {{#author}}
            <a href="/posts/update/{{post.id}}" class="post-details-btn">수정</button>
            <button type="button" class="post-details-btn" id="btn-delete">삭제</button>
            {{/author}}
        </article>
        <footer>
            <img src="/css/img/github-mark.png" alt="#">
            <a href="https://github.com/oiysful/springboot-webservice">Github Repository</a>
        </footer>
    </main>
    
    <script src="/js/common/common.js"></script>
    <script src="/js/posts/detail.js"></script>
</body>
</html>